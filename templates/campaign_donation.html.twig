{#
/**
 * @file campaign_donation.html.twig
 * Default theme implementation to present Campaign donation data.
 *
 * This template is used when viewing Campaign donation pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_campaign_donation()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('campaign_donation') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
